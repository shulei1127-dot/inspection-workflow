# AI 能力认证申报材料 — inspection-workflow（巡检工单流程自动化）

> **申报人员岗位**: 售后工程师 / 售后自动化开发
> **所属公司**: 长亭科技（Chaitin Technology）
> **项目性质**: 公司业务系统（售后巡检交付自动化平台）
> **服务地址**: http://10.20.20.208:8100 (内网)

---

## 一、AI 项目经历

### 1.1 项目概述

**inspection-workflow** 是一个面向公司**售后巡检交付业务**的全流程自动化平台。该平台覆盖巡检工单从 PTS（内部项目跟踪系统）同步 → 外包派单 → AI 邮件发送 → 工单闭环的完整生命周期，将原来需要多人在多个系统之间手动操作的流程**全面自动化**。

项目代码仓库(GitHub): [https://github.com/shulei1127-dot/inspection-workflow](https://github.com/shulei1127-dot/inspection-workflow) （请设置为公开或联系我开通查看权限）

### 1.2 业务背景与痛点

长亭科技在全国范围内为客户提供网络安全产品（雷池 WAF、洞鉴扫描器、谛听 NDR、牧云 CWPP 等）的定期巡检服务。每条巡检工单的完整处理链路涵盖：

1. **数据同步**: 从 PTS 拉取工单 → 存入本地 DB → 推送到钉钉 AITable 协作表格
2. **外包派单**: 满足条件后通过云集外包平台创建需求、指定供应商、生成订单
3. **客户邮件**: 巡检完成后，将巡检报告 PDF 用 AI 提取关键信息，自动生成邮件发送给客户
4. **工单闭环**: 检测巡检完成状态，自动在 PTS 中推进阶段并闭环工单

在系统上线前，以上环节均需人工逐条处理，效率低且易出错。上线后实现了**80%+ 的自动化处理率**。

### 1.3 我的角色与贡献

作为该项目的主要负责人，我独立完成了：

- **全栈架构设计**: FastAPI 后端 + Vue 3 前端 + PostgreSQL + APScheduler 定时任务
- **核心业务逻辑开发**: 约 15,000 行 Python 代码 + 前端 Vue 3 代码
- **AI 能力集成**: 智谱 AI（GLM-4-flash）PDF 结构化提取 + 多报告合并逻辑 + 内容智能排版
- **多系统集成**: PTS GraphQL API、钉钉 AITable (dws CLI)、云集外包平台、阿里云 SMTP
- **定时任务编排**: 6 个定时任务覆盖同步/派单轮询/邮件探测/闭环检查/Cookie 保活/邮件预分析
- **WebSocket 实时推送**: 前端实时接收同步/派单/邮件/闭环事件
- **数据库设计**: 6 张核心表 + Alembic 迁移管理
- **部署运维**: systemd 服务管理 + Docker 容器化 + 日常运维

---

## 二、AI 工具使用

### 2.1 智谱 AI (GLM-4-flash) — PDF 巡检报告智能提取

**应用场景**: 巡检工程师完成现场巡检后会提交 PDF 巡检报告（通常 10-50 页），其中包含客户信息、设备巡检结果、问题发现与建议等。传统做法需要人工阅读报告、手动填写邮件模板。

**技术实现**:

| 环节 | 方案 |
|------|------|
| PDF 文本提取 | PyMuPDF (fitz) 读取 PDF 全文 |
| AI 模型 | 智谱 AI GLM-4-flash (通过 open.bigmodel.cn API) |
| 提取字段 | 客户名称、产品名称、巡检时间、巡检数量、客户邮箱、巡检总结 |
| 输出格式 | 结构化 JSON |
| 温度参数 | 0.1（低随机性，确保提取稳定性） |

**核心 Prompt 设计**:

```python
prompt = """你是一个信息提取助手。请从以下巡检报告文本中提取以下信息：
1. 客户名称（公司全称）
2. 产品名称（谛听/洞鉴/雷池等）
3. 巡检时间（格式化为 YYYY-MM-DD）
   ——注意区分模板创建日期和实际巡检时间，优先选择正文首段的日期
4. 巡检数量（如"1套"、"4台"等，保留数字和单位）
5. 客户邮箱（可能有多个）
6. 巡检总结：提取"事件记录/巡检结果及建议"部分的完整编号列表，
   保留原文格式和换行，不要合并为一段

请严格按 JSON 格式返回。
"""
```

**关键技术挑战与解决**:

1. **模板日期 vs 实际日期**: PDF 模板创建日期常出现在页眉（如 2024 年），需通过 Prompt 引导 AI 区分页眉/封面日期与正文日期
2. **多报告合并**: 一个客户可能有多个产品（如雷池+洞鉴），每个产品各自一份 PDF 报告。需对每份 PDF 独立 AI 提取，然后智能合并：
   - 产品名用顿号拼接（"雷池、洞鉴"）
   - 数量按产品聚合（中文数字如"四台"→数字转换→合并→"八台"）
   - 总结按产品分段（`【产品名】\n总结内容`）
   - 去重：使用 `SequenceMatcher` 相似度 ≥ 0.95 判定为重复总结
3. **非巡检 PDF 过滤**: 部分记录可能包含 Word 文档、系统日志等附件，通过文件名关键词（"巡检报告"、"巡检"）识别目标 PDF
4. **内容排版**: AI 返回的总结常为连续段落，自动在数字编号前换行（正则匹配 `1、2、3、`），在章节标题前分段，提升邮件可读性

### 2.2 AI 辅助开发（Claude Code）

在项目开发过程中，大量使用 Claude Code 辅助完成：

- **代码生成**: 根据需求描述生成服务层/路由层代码框架
- **代码审查**: 发现潜在 bug 和边界条件问题
- **调试排错**: 协助分析线上异常日志并定位根因
- **文档生成**: 自动生成 CLAUDE.md 项目知识文档和运维手册
- **运维脚本**: 生成 PTS API 操作脚本（文件上传、工单推进）

---

## 三、业务成果

### 3.1 量化指标

| 指标 | 系统上线前 | 系统上线后 | 提升 |
|------|-----------|-----------|------|
| 工单同步 | 人工逐条录入 | 定时自动同步 | **100% 自动化** |
| 外包派单 | 人工登录云集逐条创建 | 条件触发自动派单 | **80%+ 自动化** |
| 巡检邮件 | 人工阅读 PDF 手动撰写发送 | AI 提取 + 自动发送 | **90%+ 时间节省** |
| 工单闭环 | 人工登录 PTS 逐条操作 | 条件触发自动闭环 | **80%+ 自动化** |

### 3.2 业务覆盖范围

- **支持产品**: 雷池(WAF)、洞鉴(扫描器)、谛听(NDR)、牧云(CWPP)、万象(BAS) 5 大产品线
- **覆盖战区**: 华东、华南、华中、华北东北、西南西北、金融头部、政府头部、通信头部 8 个战区
- **外包供应商**: 10 家（平云、腾云、禹赫、中朔、迈望、图融、微中鑫、华宇、云申、德瑞）
- **工程师覆盖**: 约 60 名交付工程师的区域映射

### 3.3 系统运行质量

- **7×24 稳定运行**: 通过 systemd 管理，定时任务自动执行
- **定时任务**: 6 个 APScheduler 任务（PTS 同步每天 16:00、派单轮询每 5 分钟、邮件探测每 2 小时、闭环检查每天 10:00、Cookie 保活每 3 小时、邮件预分析每天 9:00）
- **实时监控**: WebSocket 推送 + 前端仪表盘实时展示状态
- **API 端点**: 20+ 个 REST API 端点 + WebSocket 实时事件

### 3.4 技术创新亮点

1. **邮件预分析 + 直接发送** 双模式: 预分析提前用 AI 提取信息并缓存，发送时复用结果，避免重复调用 AI API，节省成本且提升响应速度
2. **智能 PDF 分类**: 自动识别巡检报告 PDF（文件名关键词匹配），过滤非巡检文档
3. **多报告智能合并**: 中文数量词合并算法（`四台` + `四台` → `八台`），去重算法（相似度 ≥ 95% 过滤）
4. **Cookie 自动保活**: 云集 Session 每 3 小时自动访问页面，保持登录状态有效
5. **PTS API 内网适配**: 解决 GraphQL `$variable` 参数化不支持问题，实现内联变量查询 + 频率控制（4 req/s）
6. **AITable singleSelect 中文兼容**: 发现并解决钉钉 AITable 单选字段必须使用中文显示名而非 option ID 的问题

---

## 四、Git 代码仓库

### 仓库信息

- **仓库地址**: [https://github.com/shulei1127-dot/inspection-workflow](https://github.com/shulei1127-dot/inspection-workflow)
- **项目语言**: Python 3.10+ / TypeScript / Vue 3
- **代码规模**: 约 15,000 行 Python + 前端 Vue/TS 代码
- **目录结构**: `apps/api/`(后端路由), `core/`(配置/数据库), `models/`(数据模型), `services/`(业务逻辑), `scheduler/`(定时任务), `frontend/`(Vue 前端)

### 核心技术栈

| 层级 | 技术选型 |
|------|---------|
| 后端框架 | FastAPI + Uvicorn |
| ORM | SQLAlchemy 2.0（异步风格） |
| 数据库 | PostgreSQL 14+ (psycopg v3) |
| 迁移 | Alembic |
| 前端 | Vue 3 + TypeScript + Vite + Element Plus + ECharts |
| 定时任务 | APScheduler (BackgroundScheduler) |
| AI 服务 | 智谱 AI GLM-4-flash（PDF 文本结构化提取） |
| PDF 解析 | PyMuPDF (fitz) |
| 浏览器自动化 | Playwright（PTS 文件上传/闭环） |
| 邮件 | SMTP_SSL (阿里云) |
| 钉钉交互 | dws CLI (AITable CRUD) |
| 部署 | systemd / Docker Compose |

### 建议查看的关键文件

- `services/email_pre_analysis.py` — AI 提取 + 多报告合并核心逻辑
- `services/email_sender.py` — 智谱 AI 调用 + Prompt 设计 + 邮件发送
- `services/yunji_dispatch.py` — 云集外包自动派单全流程
- `services/sync_service.py` — PTS→本地→AITable 同步流水线
- `services/pts_closure_service.py` — PTS 自动闭环逻辑
- `apps/api/main.py` — FastAPI 应用入口

---

## 五、补充说明

### 5.1 项目与岗位职责强相关性

本项目完全服务于公司售后巡检交付业务，与我的岗位职责（售后技术支持/自动化）直接相关：

- **客户需求驱动**: 随着公司巡检业务量增长，手动处理工单的效率瓶颈日益突出，自动化需求来自售后团队实际的业务压力
- **业务系统集成**: 涉及公司内部 PTS、云集、钉钉 AITable 等多个核心业务系统的深度集成
- **客户触达**: 巡检邮件直接发送给客户，邮件内容的准确性和专业性直接影响公司形象

### 5.2 项目可验证性

- 服务运行于公司内网服务器 `10.20.20.208:8100`，可通过内网访问验证
- GitHub 仓库包含完整代码、数据库迁移脚本、部署脚本、环境配置模板
- CLAUDE.md 包含详细的运维手册和故障排查指南
- 系统日志可通过 `sudo journalctl -u inspection-workflow` 查看

### 5.3 后续规划

- 实现飞书多维表格集成（扩展协作平台支持）
- PTS 自动闭环功能增强（当前多云集 Cookie 和 Playwright 依赖）
- AI 模型升级至 GLM-4 或更先进模型，提升提取准确率
- 增加巡检报告质量自动审核（AI 判断报告完整性）
